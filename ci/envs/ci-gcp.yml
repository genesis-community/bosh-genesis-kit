---
kit:
  features:
    - ((append))
    - google

genesis:
  bosh_env: gcp-uswest2-genesis-ci

params:
  # GCP Config
  google_project: snw-ocf-products

  # DNS Caching (for runtime config)
  dns_cache: true
