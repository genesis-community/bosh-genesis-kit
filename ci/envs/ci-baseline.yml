---
kit:
  name: dev
  version: latest
  features:
    - local-ha-db
    - local-blobstore
    - haproxy
    - tls
    - self-signed
    - small-footprint

params:
  env:   ci-baseline
  bosh:  genesis-ci

  availability_zones: [z1]

  disk_type: default
  base_domain:  10.200.8.32.netip.cc
  postgres_vip: 10.200.8.254
  skip_ssl_validation: true
