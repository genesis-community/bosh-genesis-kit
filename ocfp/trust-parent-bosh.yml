--- # Trust parent bosh ca certificate.
meta:
  ocfp:
    certs:
      trusted:
        - (( append ))
        - (( vault genesis.exodus_mount genesis.bosh_env "/bosh:ca_cert" )) # genesis.bosh_env is the parent director env.
