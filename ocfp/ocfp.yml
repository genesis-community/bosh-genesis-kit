---
params:
  persistant_disk_size_in_mb: 65536 # 64GiB
  trusted_certs: (( join "" meta.ocfp.certs.trusted ))

  # Required for add-secrets - see kit.yml
  ca_validity_period:   "10y"
  cert_validity_period: "10y"
